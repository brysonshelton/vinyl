<h1 class="font-bold text-4xl text-center mb-8">Artists</h1>
<div class="text-center">
  <ul>
    <% @artists.each do |artist| %>
      <li>
        <%= link_to artist.name, artist_path(artist), class: "text-blue-500 hover:underline" %>
      </li>
    <% end %>
  </ul>
</div>
